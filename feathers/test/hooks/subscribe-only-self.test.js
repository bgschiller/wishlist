const feathers = require('@feathersjs/feathers');
const subscribeOnlySelf = require('../../src/hooks/subscribe-only-self');

describe('\'subscribe-only-self\' hook', () => {
  let app;

  beforeEach(() => {
    app = feathers();

    app.use('/dummy', {
      async get(id) {
        return { id };
      }
    });

    app.service('dummy').hooks({
      before: subscribeOnlySelf()
    });
  });

  it('runs the hook', async () => {
    expect.assertions(1);
    const result = await app.service('dummy').get('test');
    expect(result).toEqual({ id: 'test' });
  });
});
