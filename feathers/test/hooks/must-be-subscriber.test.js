const feathers = require('@feathersjs/feathers');
const mustBeSubscriber = require('../../src/hooks/must-be-subscriber');

describe('\'must-be-subscriber\' hook', () => {
  let app;

  beforeEach(() => {
    app = feathers();

    app.use('/dummy', {
      async get(id) {
        return { id };
      }
    });

    app.service('dummy').hooks({
      before: mustBeSubscriber()
    });
  });

  it('runs the hook', async () => {
    expect.assertions(1);
    const result = await app.service('dummy').get('test');
    expect(result).toEqual({ id: 'test' });
  });
});
